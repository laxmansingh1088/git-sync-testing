# yaml-language-server: $schema=https://restk.ai/schema/v1/request.schema.json
# Restk Request

id: "319413717521399815"
version: 1
name: Bearer Token
method: GET
url: '{{base_url}}/me'
sortOrder: 26
description: ""
folderId: "319413717521399814"
parameters: []
headers: []
bodyType: none
bodyContentType: ""
bodyData: ""
authType: bearer
isAuthOverridden: true
authConfigData: '{"bearerToken":"{{access_token}}"}'
prescript: |-
    const token = nova.variables.get('access_token');
    if (token) { nova.request.setBearerToken(token); }
    else { nova.warn('access_token not set — run Get OAuth2 Token first.'); }
postscript: nova.test('Not 401', function() { nova.expect(nova.response.status).not.toBe(401); });
skipParentScripts: false
variables: []
