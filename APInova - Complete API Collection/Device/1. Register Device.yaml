# yaml-language-server: $schema=https://restk.ai/schema/v1/request.schema.json
# Restk Request

id: "325310386372870158"
version: 1
name: 1. Register Device
method: POST
url: '{{base_url}}/api/v0/devices'
sortOrder: 27
description: ""
folderId: "325310386372870157"
parameters: []
headers:
    - id: "E1C1C3A2-5590-48CF-AD9C-B0B683FDA8DF"
      key: Content-Type
      value: application/json
      isEnabled: true
      isSecret: false
      order: 0
bodyType: raw
bodyContentType: json
bodyData:
    rawJSON: |-
        {
                        "app_version": "1.0.0",
                        "device_id": "unique-device-identifier",
                        "device_name": "My MacBook Pro",
                        "device_type": "desktop",
                        "metadata": {
                          "processor": "M3 Pro",
                          "screen_size": "16 inches"
                        },
                        "os": "macOS",
                        "os_version": "14.0"
                      }
authType: bearer
isAuthOverridden: true
authConfigData: |-
    {
      "bearerToken": "{{REDACTED}}"
    }
prescript: ""
postscript: |-
    if (nova.response.status === 201 || nova.response.status === 200) {
        const response = nova.response.json();
        nova.collectionVariables.set('device_id', response.data.device.device_id);
    }
    nova.test('Device registration successful', () => nova.expect([200, 201]).toContain(nova.response.status));
skipParentScripts: false
variables: []
