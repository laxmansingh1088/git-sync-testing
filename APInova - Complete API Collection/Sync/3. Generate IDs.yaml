# yaml-language-server: $schema=https://restk.ai/schema/v1/request.schema.json
# Restk Request

id: "325310386381258754"
version: 1
name: 3. Generate IDs
method: POST
url: '{{base_url}}/api/v0/sync/ids/generate'
sortOrder: 2
description: ""
folderId: "325310386377064489"
parameters: []
headers:
    - id: "BC663A10-6B5D-4367-B7B7-D5508CBFEAC2"
      key: Content-Type
      value: application/json
      isEnabled: true
      isSecret: false
      order: 0
bodyType: raw
bodyContentType: json
bodyData:
    rawJSON: |-
        {
                        "count": 5
                      }
authType: bearer
isAuthOverridden: true
authConfigData: |-
    {
      "bearerToken": "{{REDACTED}}"
    }
prescript: ""
postscript: |-
    if (nova.response.status === 200) {
        const response = nova.response.json();
        if (response.data.ids && response.data.ids.length > 0) {
            nova.collectionVariables.set('entity_id', String(response.data.ids[0]));
        }
    }
    nova.test('IDs generated', () => nova.expect(nova.response.status).toBe(200));
skipParentScripts: false
variables: []
